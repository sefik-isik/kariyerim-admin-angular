/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterUniversityFacultyPipe } from './filterUniversityFaculty.pipe';

describe('Pipe: FilterUniversityFacultye', () => {
  it('create an instance', () => {
    let pipe = new FilterUniversityFacultyPipe();
    expect(pipe).toBeTruthy();
  });
});
